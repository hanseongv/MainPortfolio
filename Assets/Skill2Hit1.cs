using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Skill2Hit1 : MonoBehaviour
{
    private void OnEnable()
    {
        //Debug.Log(Time.time);
    }

    private void OnDestroy()
    {
        //Debug.Log(Time.time);
    }

    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}